# Supabase Configuration
# Get these values from your Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Admin password for /admin page
NEXT_PUBLIC_ADMIN_PASSWORD=your-secure-password

# Note: Create a storage bucket named "media" in your Supabase dashboard
# and set it to public for the media gallery to work
# Also run the SQL schema from supabase-schema.sql in the SQL Editor
