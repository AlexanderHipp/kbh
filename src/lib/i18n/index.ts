export { dictionary, type Locale, type Dictionary } from "./dictionary";
export { LocaleProvider, useLocale } from "./use-locale";
// Note: getLocale is server-only, import from "@/lib/i18n/get-locale" directly
